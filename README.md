# Coyote 2.0 Code Punisher

免责声明

重要：本项目仅供技术研究、软件开发与个人实验使用。

Coyote 2.0 Code Punisher 会通过 BLE 控制兼容设备输出电脉冲，并支持根据 VS Code 的诊断、编译失败及保存事件自动触发输出。用户应充分了解所连接设备及其输出特性，并自行确认设备适用于当前使用场景。

本项目作者及贡献者不对因使用本插件、修改源码、连接第三方设备、设备故障、协议实现差异、配置错误、自动触发、异常断连或其他不可预见情况所造成的任何直接或间接损失、设备损坏、人身伤害或其他后果承担责任。

插件内置的强度、时长、波形及协议范围限制属于软件层面的安全措施，不构成对设备实际输出或使用安全性的保证。不同硬件、固件版本及设备状态可能导致实际行为与软件预期存在差异；不得仅依据本插件中的限制判断设备是否安全。

使用自动触发功能前，请确认：

设备处于适当且可控的使用状态；
已了解设备制造商提供的安全说明及限制；
已正确配置强度、时长、冷却时间等参数；
能够在异常情况下立即停止设备输出；
不将自动触发功能用于无法随时人工干预的场景。

如出现异常输出、设备行为异常、无法停止或任何不确定情况，应立即停止使用并断开设备。

使用本项目即表示你理解并接受上述风险，并自行承担使用本项目及相关设备产生的责任。

> 让每一个 Bug 都留下深刻印象。

适用于郊狼2.0的控制器，主动控制或在保存文件或编译失败时根据错误数量自动触发电脉冲输出。

## 功能

- 连接 BLE 设备（设备名前缀 D-LAB）
- 概览 / 波形 / 规则 / AI 场景四页控制台，固定显示通道状态与停止按钮
- 波形卡片、离线播放预览、可拖动时间轴（计划参数，并非硬件测量）
- 固定、对数—幂律、多阶段阶跃映射，支持 A / B / AB 输出
- 规则试算、自定义阶跃阈值、工作区预设保存与加载
- 错误变化、连续无错检查记录、最近事件时间线
- 本地 MCP 接入 DeepSeek Harness，AI 提案在侧边栏应用后执行
- 冷却倒计时实时显示在侧边栏
- 紧急停止按钮 + 快捷键 Ctrl+Alt+S
- 所有参数在侧边栏 UI 配置，不依赖 VS Code settings.json

## 自动触发

在规则页保存并开启「自动触发」后，以下事件会安排一次延迟检查：

1. **保存文件**：等待 500ms 后读取诊断；继续编辑会取消待处理检查。
2. **构建任务启动**：识别 build 分组或名称包含 build / compile / tsc / webpack / vite 的 VS Code Task。
3. **构建任务失败结束**：补读后续编译诊断，相同错误集合不重复执行。

诊断变化只更新计数，不直接启动输出。终端手动运行的命令若不是 VS Code Task，不在任务监听范围内。
可选择当前文件或工作区，以及仅统计相对扩展启动时诊断快照的新增错误；位置或错误文本改变也会视为新诊断。
语言服务没有统一的“诊断已完成”事件，500ms 检查不保证覆盖所有延迟报告。
配置按工作区保存，自动触发每次重新加载默认关闭。

### 错误数映射

连续模式采用 S = base + A × ln(1 + 0.15 × e^1.4)，A 根据用户上限归一化到 100 个错误。
阶跃模式默认 1–3 提醒、4–15 警示、16+ 疏密变化，可在规则页更改两个阈值；这些是可调默认值，不代表错误数量与故障严重性的统计结论。
关闭随错误数调整时固定使用基础值。0 个错误不自动输出，自动 / AI 场景受规则中的强度和时长上限限制。
阶跃内建波形采用可重复序列，使预览与实际发送计划一致。

每次触发后进入冷却期（默认 15 秒），冷却时间内所有事件忽略，侧边栏显示倒计时。

## 安全机制

- 手动、自动和 AI 场景共用串行写入与取消机制；自动 / AI 场景受规则上限及冷却限制
- 手动模式独立设置 A/B 强度（0–200）、波形通道、时长（100–30000ms）和帧间隔（20–5000ms），不使用自动规则的上限或冷却
- 波形 Z 值自动钳制到 15 以下（Z>20 脉冲宽度超 100us，产生刺痛）
- 紧急停止取消待执行场景、清除定时器、使旧发送失效并归零；侧边栏停止全部还会关闭自动触发
- 设备断开时自动清除全部状态
- CoyoteSafety 独立于 UI，即使绕过 webview 也不会超过协议上限 2047

## 连接状态与排查

点击「连接设备」后，侧边栏显示搜索设备、GATT 连接、读取服务与特性、读取电量、订阅通知等阶段及等待时长，VS Code 同时显示连接进度通知。初始化完成才显示「已连接」，期间不能重复发起连接。

连接失败时会保留失败阶段和底层错误，并显示「重试连接」。请检查电脑蓝牙、设备电源和距离，以及手机 App 或其他程序是否占用设备。等待超过 15 秒会显示排查提示，这只是等待提醒，不代表操作已超时或被取消。意外断开也会在顶部显示提示。

## 手动操作

在「波形」页顶部的手动控制区操作，无需去规则页保存：

- 设置 A/B 强度后点击「设置 A/B 强度」，立即写入设备，不要求波形正在运行。
- 勾选「实时调节」后，滑块、数字框、加减按钮的修改会自动写入；运行中的波形不会重新计时。
- 波形输出通道可独立选 A、B、A+B，并可在运行中切换。强度设置和波形激活分别控制：有强度但无波形时不持续输出。
- 「停止波形」或手动波形到时会结束波形并保留强度设置；「停止全部」将两通道强度归零。
- 顶部及手动区即时显示写入成功后的强度。设备支持通知时同步设备端变化；也可点击「从设备读取」回读并填入输入框。

强度显示是控制值，不是输出电压或体感测量。修改自动规则不会覆盖手动输入或中断手动波形。

## 从源码安装

    npm install -g @vscode/vsce
    cd vscode-coyote-punisher
    vsce package

安装生成的 .vsix 文件即可。

## 项目结构

    src/
    ├── extension.js              # 插件入口，注册命令与事件监听
    ├── coyote/
    │   ├── CoyoteProtocol.js      # BLE 数据包编码/解码
    │   ├── CoyoteController.js   # 设备连接、强度/波形控制、惩罚触发
    │   └── CoyoteSafety.js        # 安全阈值钳制
    └── ui/
        └── CoyoteSidebarProvider.js # 侧边栏 Webview、惩罚配置 UI

新增模块：`src/coyote/rules.js` 统一试算与执行映射，`SceneRuntime.js` 管理串行写入和取消，
`src/mcp/server.js` 是独立 stdio MCP 入口，`resources/dashboard.*` 是界面资源。

## AI 场景接入与验证

操作说明及 Harness 配置见 [AI 场景接入](docs/AI-scenes.md)。
需要在设备所在电脑运行 VS Code 和 MCP 适配器；不需要在此插件中保存模型 API Key。

```sh
npm install
npm test
npm run test:ui
```

界面测试在 Windows 使用已安装的 Edge；其他平台需先安装 Playwright Chromium。
测试使用模拟 BLE，不会连接或启动真实设备。

## 许可证

MIT
